function solution(s) {
    var n=0;
    if(s.length==4 || s.length==6) {
        for(var i=0; i<s.length; i++){
            if (s.charCodeAt(i)<48 || 57<s.charCodeAt(i)){
                return false;
            }
        }
        return true;
    }
    else
        return false;
}
